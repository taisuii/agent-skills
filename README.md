项目的Agent自进化能力如何，自进化能力就是，项目包含完整的错误排查日志系统，错误可复现，需求可验证，等等，有新的需求到达的时候，AI Agent可以根据需求，自行逆向，测试，开发，验证，发版，有新的错误日志复制过来的时候，可以自行排错，举个例子：
1. 用户提出需求->分析需求是否成果可验证，如果不可以验证，需要反问客户怎么验证，如果可以验证则->逆向，测试，开发，验证，发版
2. 用户提出问题的日志->分析日志是否能定位问题，若不能，则优化日志系统，让用户再次复现问题，将日志发过来，然后处理问题，修复问题，验证修复，发版本
其中，
1. 需要有能定位问题，噪声少的日志（AI Agent可以自主读取）
2. 需要有数据读取能力，包括AI Agent读取日志，AI Agent读取数据库，等密钥，只要是Agent所处的环境，都是测试环境，密钥可以随意读取，这里需要Agent自行封装给自己用的mcp（包含人类可操作的能力边界）


完全懂,而且这是对的工程原则。

你说的是:fail loud,不要 fail silent。当主方案(强健壮的那个)还没完全打磨好时,如果我用不稳定的次方案偷偷兜底,就会出现"时好时坏"——而这种间歇性正常恰恰是最难排查的:你以为主方案在工作,其实是兜底在顶着;主方案真正的失效点被掩盖,既测不出来也没法针对性优化。宁可干净地坏掉,也不要脏兮兮地"还能用"。 在 hook/逆向这种需要精确定位失效锚点的场景尤其致命。

可以接受你在对话之后没有成果，但是我希望你能摸清整个流程，在长远看来是有利的，不然就会改很多个版本
你可以打日志，没关系，我会配合你调查根因，你也可以埋点，我给你发日志，但是不能乱猜和臆想，还有就是无意义的兜底



然后现在，再做最后一轮审查吧，不关心安全问题，只考虑功能是否闭环，多站点，多配置
逻辑是否闭环，是否自洽，清晰，对接方是否能无阻碍顺利对接
然后为我这套架构评分，可维护性，可扩展性，可用性，接入便捷性
收收尾，将这些工作做好，项目要有规范性，秩序性，不能一团乱麻，不能胡乱兜底。
fail loud,不要 fail silent。当主方案(强健壮的那个)还没完全打磨好时,如果我用不稳定的次方案偷偷兜底,
就会出现"时好时坏"——而这种间歇性正常恰恰是最难排查的:你以为主方案在工作,其实是兜底在顶着;主方案真正
的失效点被掩盖,既测不出来也没法针对性优化。宁可干净地坏掉,也不要脏兮兮地"还能用"。
你可以充分发挥你的主观能动性，自检自省客观的去检验和分析，不能马虎！



你现在是该项目的 AI 架构与协作工程师。你的任务是：为当前仓库搭建一套“AI 可读、可维护、可扩展”的项目认知与协作文件体系，风格参考成熟多项目协作仓库（含 rules / skills / agents 文档体系）。
请严格按以下要求执行：

# 一、目标
1) 让后续 AI 在新会话中能快速理解项目结构、技术栈、边界与约束。
2) 让 AI 在开发时有统一执行规范（代码风格、接口约定、联调原则、文档同步要求、遇阻上报机制）。
3) 让 AI 具备“会话记忆能力”：能读历史上下文并在结束时追加关键记忆。
4) 输出内容必须可落地：直接生成文件、目录、模板与示例内容，不要只给建议。

# 二、请先做项目扫描（必须）
请先扫描仓库并总结：
- 顶层目录与每个子项目职责
- 技术栈（前端/后端/管理端/脚本）
- 运行与构建方式（从 package.json、pyproject、requirements、Docker、Makefile 等提取）
- 核心业务模块与数据流
- API 风格、鉴权方式、配置管理方式
- 现有文档与规范文件（如 README、CONTRIBUTING、docs）

然后输出《项目事实基线》作为后续生成依据。

# 三、生成的目录与文件（必须创建）
在仓库中创建并填充以下文件（若已存在则增量更新，不破坏已有有效内容）：

1) .cursor/rules/development.mdc
- 项目统一开发规则
- 必含：执行纪律、禁止糊弄、遇阻必报、前后端联动、返回体约定、环境变量约束、提交前自检、DoD

2) .cursor/skills/project-architecture/SKILL.md
- 全项目架构说明
- 必含：项目结构树、技术栈表、子项目边界、关键链路、常见改动入口

3) .cursor/skills/ai-context-memory/SKILL.md
- 会话记忆机制说明
- 必含：何时读 memory、何时写 memory、记录模板、去重与归档策略

4) .cursor/skills/ai-context-memory/memory.md
- 初始化长期记忆文件
- 先写 3~5 条“当前项目已知关键信息”示例，按时间倒序格式

5) .cursor/skills/project-development-log/SKILL.md
- 开发日志规范（记录每次改动目的、范围、影响、验证）

6) AGENTS.md
- 给 AI Agent 的项目级工作手册
- 内容与 development.mdc、architecture skill 保持一致但更偏执行指南

7) CLAUDE.md（或同类 agent 入口文档）
- 面向 AI 的“开工必读”
- 必含：先读哪些文件、不同子项目进入前要读什么、常见陷阱

8) doc/ai-onboarding.md
- 人类/AI 共读的项目上手文档
- 必含：5分钟理解项目、10分钟跑通、20分钟开始改需求

# 四、文件内容质量要求（必须）
1) 所有规范都要“可执行”，避免空话。
2) 必须提供模板段落，例如：
- 需求澄清模板
- 受阻汇报模板（现象/尝试/结论/选项）
- 开发完成回报模板
3) 必须提供“反例 vs 正例”（例如危险配置回退、接口返回不一致、跳过验证）。
4) 规则中必须约束：
- 关键依赖配置（数据库/缓存主机）不得使用 localhost 隐式回退
- 不确定时先查证或提问，不得猜测实现
- 代码变更影响架构时必须同步更新对应 skill 文档
5) 给出“最小验证清单”（接口、页面、构建、lint）。

# 五、多子项目支持（如 monorepo）
如果是 monorepo，请额外为每个子项目生成：
- <subproject>/.cursor/skills/project-architecture/SKILL.md
- （若需要）<subproject>/.cursor/rules/development.mdc
并在根级 architecture skill 中建立索引与联动关系。

# 六、输出格式（必须）
请按以下格式输出：
1) 你识别到的项目事实基线（简洁）
2) 将创建/更新的文件清单
3) 每个文件的核心内容摘要
4) 已落地的文件内容（直接写入仓库）
5) 后续维护建议（如何让 AI 记忆长期有效）

# 七、执行原则
- 不要只给方案，直接落地到文件。
- 不要覆盖用户已有有效规则，优先增量合并。
- 保持中文简体输出。
- 术语统一、路径准确、可复制使用。

附加约束：
- 生成内容必须贴合当前仓库真实结构，不允许写与仓库不符的目录。
- 所有示例命令与路径需可在当前项目直接使用。
- 若发现信息不足，先列出缺失项并基于已知信息给出“可运行的初版”，不要停在提问阶段。
- 完成后给出一个“AI 首次接手任务”的标准流程（读取顺序 + 执行顺序 + 回写顺序）。
